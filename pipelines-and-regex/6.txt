Пребарај ги сите фајлови во моменталната локација и пресметај ја просечната големина на тие фајлови користејќи awk. Исклучи ги директориумите.

Решение:
ls -l | grep '^-' | awk '{sum += $5; count++} END {if (count > 0) print sum/count}'